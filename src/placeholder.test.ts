import { describe, expect, it } from 'vitest';

// Trivial placeholder proving the test runner actually runs, per #4's
// acceptance criterion 1. Replaced once #5 lands real behaviour to test.
describe('scaffold', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
